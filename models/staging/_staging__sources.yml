version: 2

sources:
  - name: jaffle_shop
    database: RAW
    schema: trouze
    tables:
      - name: raw_customers
        loaded_at_field: "cast(LOAD_DTS as TIMESTAMP)"
      - name: raw_orders
        loaded_at_field: "cast(LOAD_DTS as TIMESTAMP)"
      - name: raw_products
        loaded_at_field: "cast(LOAD_DTS as TIMESTAMP)"
      - name: raw_stores
        loaded_at_field: "cast(LOAD_DTS as TIMESTAMP)"
      - name: raw_supplies
        loaded_at_field: "cast(LOAD_DTS as TIMESTAMP)"
      - name: raw_items
        freshness:
        loaded_at_field: "cast(LOAD_DTS as TIMESTAMP)"        
